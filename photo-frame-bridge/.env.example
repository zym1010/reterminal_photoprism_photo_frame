# Copy to .env (gitignored, never commit) and fill in your own value for local
# testing. Not read by the Docker image itself - that gets env vars from
# Container Manager's Environment tab instead (see README.md).
TODOIST_API_TOKEN=your_token_here
