# ESPHome secrets (WiFi credentials, API/OTA/AP passwords) - never commit
esphome/secrets.yaml

# ESPHome build artifacts
esphome/.esphome/

# Python virtualenvs
.venv/
.esphome-venv/
__pycache__/
*.pyc

# Docker export artifacts (rebuild locally instead of committing binaries)
*.tar

# macOS
.DS_Store

# Personal photos - local-only staging area for the adhoc_images bridge feature,
# never belongs in the repo. Real copies live on the NAS (see photo-frame-bridge/README.md).
kid_photos/
